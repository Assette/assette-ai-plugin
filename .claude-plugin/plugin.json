{
  "name": "assette",
  "version": "0.14.0",
  "description": "Assette Implementation — Node.js MCP shim + the plugin-config skill. The author-facing skills (PowerPoint / Excel / Word smart-page fabricators, Data Doc author, block author, data-object author, content classification, plus general orientation) are delivered by the hosted server after sign-in via 'initialize assette' / 'update assette'.",
  "author": {
    "name": "Assette"
  },
  "skills": [
    "./skills/assette-plugin/"
  ],
  "mcpServers": {
    "assette": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/assette-mcp-shim.bundle.js"],
      "env": {
        "ASSETTE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
